<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Estrader" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/color_main</item>
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <!-- Applying custom font-family. -->
        <item name="android:textViewStyle">@style/customTextViewStyle</item>
        <item name="android:buttonStyle">@style/customButtonStyle</item>
        <item name="android:editTextStyle">@style/customEditTextStyle</item>
        <item name="android:radioButtonStyle">@style/customRadioButtonStyle</item>
        <item name="android:checkboxStyle">@style/customCheckboxStyle</item>
        <item name="android:alertDialogTheme">@style/AppTheme.AlertDialogTheme</item>
    </style>

    <style name="Theme.Estrader.NoActionBar">
        <item name="windowActionBar">false</item>

        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Estrader.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Estrader.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>